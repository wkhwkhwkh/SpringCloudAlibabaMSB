server:
  port: 9002

spring:
  application:
    name: albaba-nacos-provider
  cloud:
    discovery:
      server-addr: localhost:8848


management:
  endpoint:
    web:
      exposure:
        include: '*'
